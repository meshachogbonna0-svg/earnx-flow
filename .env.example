# Server-only Gemini configuration
# Add this variable to your local .env file (never commit the real value):
GEMINI_API_KEY=

# Optional model override; the server defaults to gemini-2.5-flash-lite.
GEMINI_MODEL=gemini-2.5-flash-lite
